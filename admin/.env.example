HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Seeds the default Strapi catalog locale on first boot. Polish is the catalog
# default per the catalog content contract; the bootstrap in src/index.ts also
# ensures English (en) exists as a secondary locale.
STRAPI_PLUGIN_I18N_INIT_LOCALE_CODE=pl
