SUPABASE_URL=#
SUPABASE_KEY=#

# Strapi catalog (server-only — never read from client code).
# STRAPI_API_URL is the base URL of the Strapi instance, e.g. https://your-project.strapiapp.com
# STRAPI_API_TOKEN is a read-only API token issued in Strapi → Settings → API Tokens.
STRAPI_API_URL=#
STRAPI_API_TOKEN=#
