---
// Legacy compatibility shim. SendLog's visible auth flow is passwordless
// magic link — see `context/changes/passwordless-auth-flow/plan.md`. The
// legacy password `/api/auth/signup` endpoint still redirects here, so this
// page forwards into the unified passwordless post-request landing page
// rather than describing an obsolete email-confirmation flow.
return Astro.redirect("/auth/check-email", 307);
---
